server:
  port: 8081
  ssl:
    enabled: true
    key-store-type: PKCS12
    key-store: /Users/ivanberezuckij/work/pet-projects/logistic-center/backend/user-service/certificate.p12
    key-store-password: 1PwCgNAb

grpc:
  server:
    port: 9090

spring:
  kafka:
    bootstrap-servers: localhost:29092
    group-id: userservice
  consumer:
    properties:
      spring.json.trusted.packages: "ru.itpark.userservice.domain.user"


  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://auth.dppmai.ru/realms/dpp-platform
          jwk-uri: https://auth.dppmai.ru/realms/dpp-platform/protocol/openid-connect/certs
  liquibase:
    user: userservice
    password: 12345
    change-log: 'classpath:db/changelog/db-init.yaml'
  datasource:
    url: "jdbc:postgresql://localhost:7432/userservicedb"
    username: userservice
    password: 12345
#    hikari:
#      transaction-isolation:

keycloak:
  serverUrl: https://auth.dppmai.ru
  realm: dpp-platform
  clientId: admin-cli
  username: admin
  password: HVG-Rqf-5PV-Lp6
  realmName: dpp-platform

springdoc:
  api-docs:
    path: /api-docs

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} %highlight(%-5level) %logger{36} %M - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} %M - %msg%n"
  file:
    name: log.txt
  level.root: info
